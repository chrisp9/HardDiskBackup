<mui:ModernWindow x:Class="HardDiskBackup.View.BackupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             xmlns:VM="clr-namespace:HardDiskBackup"           
             mc:Ignorable="d" d:DesignWidth="300"
             Style="{StaticResource BlankWindow}" Height="90.458">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="A backup has been scheduled..." Grid.Row="0" Style="{StaticResource Heading2}" />
        <TextBlock Text="Please plug in your backup device to continue" Grid.Row="1" Width="292" />

        <ProgressBar Grid.Row="2"/>
    </Grid>

</mui:ModernWindow>
